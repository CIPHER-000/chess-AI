# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Supabase (replaces direct PostgreSQL)
supabase==2.3.0
postgrest-py==0.10.6
# supabase-auth is automatically installed as dependency of supabase
# Removed explicit version 2.12.4 (doesn't exist - use supabase's dependency)

# Database (SQLAlchemy still used for ORM)
sqlalchemy==2.0.23
alembic==1.13.0
asyncpg==0.29.0

# Redis for caching and task queue
redis==5.0.1
celery==5.3.4

# Chess-related libraries
python-chess==1.999
stockfish==3.28.0

# HTTP requests
httpx==0.24.1  # Compatible with supabase 2.3.0 (requires <0.25.0)
requests==2.31.0

# AI Model Providers
openai==1.12.0  # Optional: for OpenAI provider

# Data processing
pandas==2.1.4
numpy==1.26.2

# Configuration and environment
pydantic==2.5.1
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Date/time handling
python-dateutil==2.8.2

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-mock==3.12.0
pytest-cov==4.1.0

# Development tools
black==23.11.0
flake8==6.1.0
mypy==1.7.1

# Logging
loguru==0.7.2

# Background tasks
dramatiq[redis]==1.15.0

# PDF generation for reports
reportlab==4.0.7
matplotlib==3.8.2
seaborn==0.13.0
